import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        try {
            Scanner fileScan = new Scanner(args[0]);
            boolean isStart = true;
            while (fileScan.hasNext()) {
                if (fileScan.next() == "#") {

                }
            }
        }
    catch(Exception e){

    }
}
